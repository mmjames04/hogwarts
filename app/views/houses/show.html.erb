<h1><%= @house.name %></h1>
<% @students.each do |student| %>
<% if student.house_id == @house.id %>
<div class="student">
  <h2><%= student.name %></h2>
  <%= image_tag student.img_url %>
  <% end %>
</div>
<% end %>